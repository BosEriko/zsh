# =========================================================================== [Installation] ===== #

if [[ "$OS_TYPE" == "win" ]]; then
  # Install Fastfetch
  sudo apt-get update
  sudo apt-get install -y fastfetch
fi
